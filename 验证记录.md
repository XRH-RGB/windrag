# 本次验证记录

日期：2026-09-10。

- Python 自动化：27 项通过。覆盖原有 RAG 回归、启动端口复用、会话、风场隔离、用户管理和完整运维链路。
- 原有免登录知识演示脚本：通过，覆盖直出分支、FAQ、角色与有效阈值。
- 独立汇报脚本：通过，覆盖 9 章节、3 种流程、9 人轮播以及清洗、统计、预测、异常、处理与摘要实验。
- JavaScript 语法：app.js、ops.js 通过。
- 浏览器：管理员、运维人员、普通用户登录通过；管理员驾驶舱与数据图表已查看。
- 浏览器：实际选择 demo_wind_data.csv 文件上传，预览显示 26 行；确认导入后清洗，处理结果与前后预览正常。
- 浏览器：实际运行预测、生成异常告警、保存告警处理、新增维修记录，日报已收录新增记录。
- 浏览器：运维账号无用户管理入口，可执行检测；普通用户无用户管理入口和预测执行按钮，只查看预测结果。
- 浏览器控制台：所检查业务页面未发现 JavaScript error 日志。

独立本地 HTML 的浏览器访问被工具的 file URL 策略拦截，因此汇报文件采用脚本交互测试和资源检查验证，未声称完成该文件的浏览器视觉验收。业务系统的 HTTP 页面已实际检查。

模型边界：未提供 BERT 权重及外部模型服务，本次没有真实 BERT / LLM 模型精度测试；系统默认规则、FAQ、BM25 与数据摘录。预测使用可运行的持续性时间序列基线，非生产预测模型。设备状态来自历史观测快照。
